CREATE TABLE CATEGORIA (IDCATEGORIA BIGINT NOT NULL, NOME VARCHAR(255), PRIMARY KEY (IDCATEGORIA))
CREATE TABLE EQUIPAMENTO (ID INTEGER NOT NULL, DISPONIVEL TINYINT(1) default 0, NOME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE USUARIO (IDUSUARIO BIGINT NOT NULL, LOGIN VARCHAR(255), NOME VARCHAR(255), SIAPE VARCHAR(255), STATUS TINYINT(1) default 0, PRIMARY KEY (IDUSUARIO))
CREATE TABLE USUARIO_CATEGORIA (Usuario_IDUSUARIO BIGINT NOT NULL, listCategoria_IDCATEGORIA BIGINT NOT NULL, PRIMARY KEY (Usuario_IDUSUARIO, listCategoria_IDCATEGORIA))
ALTER TABLE USUARIO_CATEGORIA ADD CONSTRAINT FK_USUARIO_CATEGORIA_listCategoria_IDCATEGORIA FOREIGN KEY (listCategoria_IDCATEGORIA) REFERENCES CATEGORIA (IDCATEGORIA)
ALTER TABLE USUARIO_CATEGORIA ADD CONSTRAINT FK_USUARIO_CATEGORIA_Usuario_IDUSUARIO FOREIGN KEY (Usuario_IDUSUARIO) REFERENCES USUARIO (IDUSUARIO)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
